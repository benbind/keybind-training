
#Task 3: Select all occurrences of 'areaa' and rename them to 'area'
def compute_triangle_area(base, height):
    areaa = 0.5 * base * height
    return area

def compute_circle_area(radius):
    area = 3.14159 * radius ** 2
    return areaa

def compute_square_area(side):
    areaa = side ** 2
    return area

base, height, radius, side = 10, 5, 3, 7


triangle_area = compute_triangle_area(base, height)
circle_area = compute_circle_area(radius)
square_area = compute_square_area(side)

print("Typos fixed!")